name: Security

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: '0 6 * * 1'   # Monday 6am UTC

permissions: read-all

jobs:
  # -----------------------------------------
  # SECRETS — Gitleaks
  # -----------------------------------------
  gitleaks:
    name: Gitleaks - Secret Scan
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      security-events: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false
          fetch-depth: 0

      # gitleaks/gitleaks-action now requires a paid org license (gitleaks.io) —
      # this repo is under an org, so the Action refuses to run without
      # GITLEAKS_LICENSE. Use the free OSS CLI directly instead: same detection
      # engine, zero cost. Version+checksum from the gitleaks GitHub releases page.
      - name: Install Gitleaks
        run: |
          curl -fsSL -o gitleaks.tar.gz https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz
          echo "551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb  gitleaks.tar.gz" | sha256sum -c -
          tar -xzf gitleaks.tar.gz gitleaks
          chmod +x gitleaks

      - name: Gitleaks
        run: |
          ./gitleaks detect \
            --source . \
            --config .github/workflows/configuration/.gitleaks.toml \
            --report-format sarif \
            --report-path gitleaks-results.sarif \
            --redact \
            --exit-code 1

      - name: Upload Gitleaks SARIF
        uses: github/codeql-action/upload-sarif@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4
        if: always() && hashFiles('gitleaks-results.sarif') != ''
        with:
          sarif_file: gitleaks-results.sarif
          category: gitleaks-secrets

  # -----------------------------------------
  # SAST — CodeQL (Python)
  # -----------------------------------------
  codeql:
    name: CodeQL - SAST
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false

      # Skips cleanly until actual Python source exists — this repo is pre-MVP
      # scaffolding. Remove this guard once the SDK has real modules. hashFiles()
      # only works in step context, so the guard lives on each step below (a
      # job-level `if: hashFiles(...)` is invalid and silently zeroes out the
      # entire workflow — no jobs get scheduled at all).
      - name: Initialize CodeQL
        if: hashFiles('**/*.py') != ''
        uses: github/codeql-action/init@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4
        with:
          languages: python
          config-file: .github/codeql/config.yml

      - name: Perform CodeQL Analysis
        if: hashFiles('**/*.py') != ''
        uses: github/codeql-action/analyze@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4

  # -----------------------------------------
  # SAST — Semgrep (OWASP + Python)
  # -----------------------------------------
  semgrep:
    name: Semgrep - SAST
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      security-events: write   # required by upload-sarif

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false

      - name: Setup Python (Semgrep CLI)
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.12'

      - name: Install Semgrep
        run: pip install semgrep

      - name: Semgrep Scan
        run: |
          semgrep scan \
            --config p/python \
            --config p/owasp-top-ten \
            --config p/security-audit \
            --config p/secrets \
            --sarif --output semgrep-results.sarif \
            . || true
          FINDINGS=$(jq '.runs[0].results | length' semgrep-results.sarif)
          echo "Semgrep findings (non-suppressed): $FINDINGS"
          if [ "$FINDINGS" -gt 0 ]; then
            jq -r '.runs[0].results[] | "[\(.ruleId)] \(.locations[0].physicalLocation.artifactLocation.uri):\(.locations[0].physicalLocation.region.startLine // "?") -- \(.message.text[0:300])"' semgrep-results.sarif
            exit 1
          fi
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}

      - name: Upload Semgrep SARIF
        uses: github/codeql-action/upload-sarif@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4
        if: always() && hashFiles('semgrep-results.sarif') != ''
        with:
          sarif_file: semgrep-results.sarif
          category: semgrep-sast

  # -----------------------------------------
  # CI/CD COMPLIANCE — Plumber
  # -----------------------------------------
  plumber:
    name: Plumber - CI/CD Compliance
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: false

      - name: Run Plumber
        uses: getplumber/plumber@5302ddc7d9f1c9edb3f617bff04a09be452bcfe2 # v0.3.79
        with:
          sarif: plumber-results.sarif
          output: plumber-results.json
          pbom-cyclonedx: plumber-pbom.cdx.json
          threshold: 90
          metadata-token: ${{ secrets.PLUMBER_METADATA_TOKEN }}
        env:
          GITHUB_TOKEN: ${{ secrets.PLUMBER_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Upload Plumber SARIF
        uses: github/codeql-action/upload-sarif@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4
        if: always() && hashFiles('plumber-results.sarif') != ''
        with:
          sarif_file: plumber-results.sarif
          category: plumber-supply-chain

      - name: Upload Plumber artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: always() && hashFiles('plumber-results.json') != ''
        with:
          name: plumber-report
          path: |
            plumber-results.json
            plumber-pbom.cdx.json

      - name: Plumber Gate (compliance >= 90% required)
        if: hashFiles('plumber-results.json') != ''
        run: |
          SCORE=$(jq '.compliance' plumber-results.json)
          echo "Plumber compliance: ${SCORE}%"
          if [ "$SCORE" -lt 90 ]; then
            echo "::error::Plumber compliance ${SCORE}% < 90%% (security gate)"
            exit 1
          fi
